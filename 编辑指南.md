# SLDX_Ambition_Website 编辑指南

## 📖 前言

本指南面向没有编程基础的团队成员，教你如何更新战队网站内容。

**重要说明**：
- 所有修改都需要通过 **Pull Request (PR)** 提交给管理员审核
- 审核通过后会由管理员合并到主分支，网站会自动更新
- **你只能在你自己 Fork 出来的仓库里折腾**。原始仓库（`SYLU-Ambition/SLDX_Ambition_Website`）你**没有**写入权限，不需要、也无法直接往上推东西；所以「不要直接修改主分支」指的是**不要在你的 Fork 里乱开分支**，见下方 [⚠️ 提交前必读](#-提交前必读新手最容易踩的三个坑)。

**🌿 分支约定（一句话版本）**：

> **认准 `main` 分支，就在 `main` 上改，改完直接提交推送。不要新建分支。**

---

## 🤖 小白强烈建议：配合 AI Agent 一起食用

**如果你没有任何编程/命令行经验，别硬扛——把 AI 助手当成你的"副驾驶"。**
本指南里绝大多数卡住人的地方（代理配不好、命令报错看不懂、路径写错、PR 页面选不明白），
**AI 助手都能在几十秒内帮你定位**，比自己在群里等回复快得多。

### 推荐用什么

| 工具 | 说明 |
| --- | --- |
| **DeepSeek Harness / Claude Code 这类能读写文件的 Agent** | ⭐ **最推荐**。它能直接打开你本地的项目文件夹，帮你改 `.md`、放图片、跑 `pnpm dev`、执行 git 命令并看懂报错 |
| **VS Code 自带的 Copilot Chat** | 也不错，在编辑器里按 `Ctrl+Alt+I` 就能问；它对当前打开的文件有上下文 |
| **网页版 AI（DeepSeek / 豆包 / Kimi / ChatGPT 等）** | 应急可用，但它**看不到你的文件、也不能帮你执行命令**，只能给你"照着做"的答案 |

> 💡 **没装过 Agent 也没关系**：下载一个能操作本地文件的 AI 编程工具，安装后**把 `SLDX_Ambition_Website` 文件夹打开**，
> 就可以直接对话了。这一步装好，后面能省你几个小时。

### 怎么问才有效（直接抄这些说法）

**核心原则：把「现象」和「报错原文」给它，别只说"不行了"。**

✅ **这样问（有效）**：

```
我 clone 这个仓库时卡住了，终端里的报错是：
Failed to connect to github.com port 443: Timed out
我的代理软件是 Clash Verge，系统代理模式，端口 7897。帮我解决。
```

```
我要在 doc/public/Photo/photo_24dks/ 里加 5 张照片，
请帮我把它们重命名成接着现有编号的名字，并在 doc/Photo/24DKS.md 末尾
按现有格式加上引用，路径要符合这个项目的规范。
```

```
pnpm dev 之后页面白屏了，我刚刚改过 .vitepress/sidebar.mjs。
终端报错是：<把报错粘过来>
帮我看看是不是我加的那行语法写错了。
```

```
我已经把改动推送到自己 Fork 的 main 了，现在要在 GitHub 上发 PR。
请一步一步告诉我该点哪里，base 和 compare 分别该选什么。
```

❌ **这样问（无效，AI 只能瞎猜）**：

```
我的网站坏了 / 报错了 / 弄不好
```

### 让 Agent 帮你之前，先把它"喂"上项目规矩

本仓库根目录有一个 **`AGENTS.md`**，里面写着这个项目的**硬性约定**
（哪些目录不能提交、图片压缩会产生无意义改动、Markdown 里不能用 `<template>` 等）。
主流 Agent 工具会**自动读取**它。

- 如果你的工具没自动读，就手动说一句：**「先读一下项目根目录的 AGENTS.md 和编辑指南.md，再动手」**。
- 这样它就**不会**犯那些"需要人反复叮嘱"的低级错误。

### ⚠️ 但是：AI 会出错，最终责任在你

这几条**必须自己把关**，别全权交给 AI：

1. **提交前自己看一眼「源代码管理」面板**：确认只有你改过的文件。
   AI 有可能顺手"优化"了别的文件，或者把 `node_modules` 加进来。
2. **涉及删除的操作（删除文件、`git reset`、`git push -f`）一律先拒绝，问清楚再同意**。
   本指南反复强调过：这几条命令会**永久丢掉你的改动**。
3. **AI 说"已经帮你提交/推送好了"，也要自己上 GitHub 网站确认**：
   打开你自己的仓库，看提交记录里到底有没有。
4. **不要把它自己编的东西当成项目规范**：项目规范以这份指南和 `AGENTS.md` 为准。
   AI 可能给你一套"标准 Git 流程"（比如建议你新建分支），**那和本项目的要求是冲突的**，见 [坑 2](#坑-2不要新建分支直接在-main-上改重要)。

> 🔑 **一句话总结**：**让 AI 帮你查错、解释报错、写 Markdown；
> 但"提交了什么文件""推到哪个分支""点不点那个绿色按钮"这三件事，自己看清楚了再动手。**

---

## ⚠️ 提交前必读（新手最容易踩的三个坑）

这一节是**踩过坑之后补写的**，请务必在动手之前读完。三条都属于「不读就会卡住半天」的类型。

### 坑 1：Git 连不上网 / clone 卡住不动（代理问题）

**现象**：

- `git clone` / `git push` / `git pull` 卡在 `Resolving deltas`、`Connecting to github.com`，几分钟后报
  `Failed to connect to github.com port 443: Timed out`、`Recv failure: Connection was reset`、`OpenSSL SSL_read: Connection was reset`。

**原因**：你的电脑访问 GitHub 走了代理软件（Clash / v2rayN 等），但**代理软件的分流模式不同，对 Git 的影响完全不同**：

| 代理模式 | 说明 | 终端里的 Git 能不能直接连上 GitHub |
| --- | --- | --- |
| **虚拟网卡模式**（TUN 模式） | 代理接管整台电脑的全部流量，系统层面透明转发 | ✅ 能，不需要额外设置 |
| **系统代理模式**（System Proxy / 系统代理） | 只在系统里登记一个 HTTP 代理端口，**浏览器会自动用，命令行程序不会** | ❌ 不能，**必须手动给 Git 指定代理端口** |

大部分代理软件**默认就是「系统代理模式」**，所以「浏览器能打开 GitHub，终端里的 git 却连不上」是极其常见的现象，不是你网络坏了。

**解决办法（系统代理模式下必做）**：在终端里把 Git 的代理指向你代理软件的 HTTP 端口。

先确认端口号：打开你的代理软件，看「设置 / 端口 / 混合端口」那一栏。**Clash Verge / Clash for Windows 的默认混合端口通常就是 `7897`**（老版本的 Clash 常见 `7890`，v2rayN 常见 `10809`）。**以你自己软件里显示的数字为准**，下面示例统一用 `7897`。

```bash
# 给 GitHub 单独设置代理（推荐，只影响 GitHub，不影响访问公司/学校内网）
git config --global http.https://github.com.proxy http://127.0.0.1:7897
git config --global http.https://github.com/.proxy http://127.0.0.1:7897
```

```bash
# 或者给所有 https 的 git 请求设置代理（简单粗暴，能用）
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

**换端口 / 换软件之后记得同步改**，改完用这条命令确认写进去了：

```bash
git config --global --get-regexp proxy
```

**临时用一次**（不想写进全局配置，只对当前这个终端窗口生效）：

```bash
# Windows PowerShell
$env:HTTP_PROXY="http://127.0.0.1:7897"; $env:HTTPS_PROXY="http://127.0.0.1:7897"

# Windows CMD
set HTTP_PROXY=http://127.0.0.1:7897
set HTTPS_PROXY=http://127.0.0.1:7897

# macOS / Linux
export HTTP_PROXY=http://127.0.0.1:7897
export HTTPS_PROXY=http://127.0.0.1:7897
```

**想让这个设置长期生效**（推荐，省得每次开终端都要设一遍）：

- **Linux / macOS**：把上面两行 `export` 写进 `~/.bashrc` 或 `~/.zshrc`，然后 `source ~/.bashrc`。
- **Windows PowerShell**：把 `$env:HTTP_PROXY=...` 写进 `$PROFILE` 文件（没有就 `New-Item -Path $PROFILE -Force` 建一个）。
  ⚠️ 但**不建议**在 Windows 上为了 Git 而全局设置系统环境变量 `HTTP_PROXY`：它会被所有命令行程序继承，某些本地程序（连 `127.0.0.1` 的服务）会因此抽风。Git 的代理就用上面的 `git config` 设置最干净。

**排查清单**（按顺序试）：

1. 代理软件**开着**了吗？节点**连通**吗？
2. 端口号对不对？在浏览器地址栏访问 `http://127.0.0.1:7897` 看看有没有反应（有反应说明端口是通的，报错说明端口写错了）。
3. 上面 `git config --global --get-regexp proxy` 输出里，端口是不是你软件里那个？
4. 还是不行就把代理**改成 TUN / 虚拟网卡模式**再试一次。
5. 反过来：如果代理**关掉**了、但 Git 里还留着代理配置，同样会连不上，此时需要**清掉**代理：
   ```bash
   git config --global --unset http.https://github.com.proxy
   git config --global --unset http.https://github.com/.proxy
   # 或者（对应上面"给所有 https 请求设置代理"的写法）
   git config --global --unset http.proxy
   git config --global --unset https.proxy
   ```
   > 一句话：**代理软件的开关状态，必须和 Git 里的代理配置保持一致。** 开着软件就配端口，关了软件就删配置。

> 💡 只在终端里用 Git 的同事（VS Code 的源代码管理面板本质上也是调用终端里的 git），都受这条影响。
> 如果 VS Code 里推送报错，先用 VS Code 的终端（`` Ctrl+` ``）执行一次 `git push`，看它报什么错，再对照本节。

> 🤖 **懒得自己排查？直接把报错复制给 AI Agent**，这样问一句就行：
>
> ```
> 我用 git clone / git push 时报错：<粘贴完整报错>。
> 我的代理软件是 <Clash Verge / v2rayN / 其他>，用的是系统代理模式，端口 <如 7897>。
> 这个项目在 /path/to/SLDX_Ambition_Website。请帮我确认代理该怎么配，并告诉我怎么验证。
> ```
>
> 详见 [小白强烈建议：配合 AI Agent 一起食用](#-小白强烈建议配合-ai-agent-一起食用)。

### 坑 2：不要新建分支！直接在 `main` 上改（重要）

**这是最严重、最容易把 PR 搞废的一条。**

请记住这句话：

> **克隆下来之后，你就在 `main` 分支上直接改、直接提交、直接推送。不要 `git checkout -b`，不要建新分支。**

#### ❌ 错误做法：建一个空白分支再上传所有文件

有些同学会这样做（**千万别学**）：

```bash
# ❌❌❌ 全是错误示范
git checkout --orphan my-branch   # 建一个没有历史记录的空白分支
git rm -rf .                      # 清空文件
# 把整个项目文件重新拷进来……
git add .
git commit -m "上传网站"
git push origin my-branch
```

**为什么不行**：这样推上去的分支和 `main` **没有共同的提交历史**，GitHub 会认为你「删掉了所有旧文件、新增了所有新文件」。于是：

- 这个分支**没法发 PR**，点 `Compare & pull request` 会提示类似
  **`There isn't anything to compare.`**、
  （或 `main and my-branch are entirely different commit histories`，
  GitHub 侧提示 **`This branch has conflicts that must be resolved`** 等），也就是标题里说的「差异过大不给操作」；
- 即使强行合进去，**所有人的提交历史都会被抹掉**，以后谁改了什么、什么时候改的全都查不到，出了问题也没法回滚；
- 仓库会瞬间变大（所有文件都当成新增重传一遍），构建和克隆都变慢。

另外还有两种**不要做**的做法：

- ❌ 每次改动都新建一个分支（`git checkout -b update-xxx`），改完提 PR；下一个改动再建一个分支 —— 新手非常容易搞混分支、把改动提到错误的分支上，或者 PR 之间互相冲突。**不需要这么做，我们只用 `main`。**
- ❌ 在 GitHub 网页上点编辑时选 `Create a new branch for this commit`（见 [方式一](#方式一在线编辑最简单推荐新手)），这只是网页编辑器的常规流程。对我们的新成员来说，**本地编辑请统一在 `main` 上进行**。

#### ✅ 正确做法

```bash
# 1. 克隆你自己 Fork 的仓库（注意是"你的用户名"，不是 SYLU-Ambition）
git clone https://github.com/你的用户名/SLDX_Ambition_Website.git
cd SLDX_Ambition_Website

# 2. 确认当前在 main 分支（输出里带 * 的那一行应该是 main）
git branch

#    ✔️ 如果显示 * main          → 正常，继续
#    ✖️ 如果不在 main 上         → 切回 main：
#       git checkout main

# 3. 直接改文件，改完提交
git add doc/xxx.md          # 只添加你真正改过的文件
git commit -m "docs: 更新 XX 内容"

# 4. 推送到你自己 Fork 的 main 分支
git push origin main
```

推送后到 GitHub 上发起 PR，**base 选 `SYLU-Ambition/SLDX_Ambition_Website` 的 `main`，
compare（head）选你自己仓库的 `main`**——完整点法见
[方式二第 8 步 · 在 GitHub 网站上创建 Pull Request](#8-在-github-网站上创建-pull-request逐步图解)。

#### 🤔 不小心已经建了分支 / 已经推了空白分支怎么办？

**别慌，也别急着删仓库、更不要自己删分支**。先搞清楚自己是下面哪种情况，再决定怎么办：

**先判断：在 GitHub 网站上打开你自己的仓库，左上角有一个分支下拉框（默认显示 `main`）。**
点开它，看里面除了 `main` 还有没有你建的分支。

| 你的情况 | 怎么办 |
| --- | --- |
| **只是本地建了分支，还没推送** | 不用管那个分支，**切回 `main` 继续干活**即可（VS Code 左下角状态栏点一下能切分支）。 |
| **分支推上去了，但里面没有你的有效改动** | **什么都不用做**。直接在 `main` 上重新改一遍，然后正常提交推送、正常发 PR。那个多余的分支留着不影响发 PR。 |
| **分支上有你唯一的改动，且没别的地方备份** | 别自己折腾，**把仓库页面截图发给管理员**，让管理员帮你确认怎么把改动挪回 `main`。 |
| **不确定属于哪种** | 一律**截图问管理员**。这一步判断错的代价比多问一句大得多。 |

> ⛔ **绝对不要做的事**：为了"清理干净"而删本地文件夹重克隆、点 GitHub 上的 **`Delete this repository`**、
> 或者在 `main` 上 merge 那个空白分支——**这两种操作都会让事情更难收拾**（空白分支和 `main` 没有共同历史，
> merge 进去会把历史搞乱）。
>
> 💡 **记住一个事实**：**多余的远端分支完全不影响你发 PR**。发 PR 只看 `你的 main` 和 `官方 main` 的差异，
> 你仓库里躺着多少个别的分支，官方仓库一点都不关心。所以**最省事的做法就是忽略它，直接在 `main` 上重做**。

#### 🚫 另外两条红线

- **不要 `git push -f` / `--force`**：会覆盖历史，把别人的提交冲掉。真需要强推时先问管理员。
- **不要 `git reset --hard`、不要 `git checkout .`**：在你还没搞懂它们之前，这两条命令的唯一效果就是**把你辛苦改的东西全部删掉，且无法恢复**。

### 坑 3：找不到 `Compare & pull request` 按钮 / 提示没有可比较的内容

**先分清两种现象**：

- **现象 A：只是找不到绿色按钮**（顶部没弹横幅）——这**不是错误**，手动发起就行，见下面第 1 步。
- **现象 B：页面提示 `There isn't anything to compare.`（没有可比较的内容）**——这才是真出问题了，见第 2 步。

**第 1 步：手动发起 PR（现象 A 的标准做法）**

1. 浏览器打开官方仓库的 PR 页面：`https://github.com/SYLU-Ambition/SLDX_Ambition_Website/pulls`
2. 点右上角绿色的 **`New pull request`** 按钮；
3. 页面出现 `Compare changes`，点它右边的蓝色小字 **`compare across forks`**
   （不点这个，`head repository` 下拉框里就只有官方仓库自己，找不到你的 Fork）；
4. 核对四个下拉框（**从右往左合并**）：

   | 位置 | 选什么 |
   | --- | --- |
   | `base repository` | `SYLU-Ambition/SLDX_Ambition_Website` |
   | `base` | `main` |
   | `head repository` | `你的用户名/SLDX_Ambition_Website` |
   | `compare` | **`main`** |

5. 选完后，下方会列出你改动的文件。**看到文件列表 = 成功了**，点 **`Create pull request`** 继续。

**第 2 步：提示 `There isn't anything to compare.` 怎么排查**

这句话的意思是「这两个分支之间找不到差异」，按顺序检查：

1. **`compare` 下拉框是不是选成了 `main`？**
   最常见的错误：你改动的其实在别的分支上，`compare` 却选了 `main`，那当然没差异。
   点开 `compare` 下拉框，看看里面还有哪些分支名，**试着选你实际改动的那个分支**，看文件列表有没有出来。
   - 如果这样能看到你的改动 → 说明你的改动**提交到分支上了**，见 [坑 2](#坑-2不要新建分支直接在-main-上改重要) 的处理办法。
   - 如果哪个分支都没有 → 继续第 2 点。
2. **你是不是还没提交/推送？**
   回 VS Code 看源代码管理面板，如果有文件还躺在「更改」区没提交，那 GitHub 上自然什么都没有。
   提交并推送后刷新页面。
3. **`head repository` 是不是选错了？**
   确认选的是**你自己用户名**的仓库，不是 `SYLU-Ambition`（官方仓库的 `main` 和它自己比，永远没差异）。
4. **你推送到官方仓库了吗？**
   打开你自己的仓库 `https://github.com/你的用户名/SLDX_Ambition_Website`，看首页的提交列表里有没有你刚才那条。
   没有的话说明推送没成功（可能是代理问题，见 [坑 1](#坑-1git-连不上网-clone-卡住不动代理问题)）。
5. 以上都对还是不行 → **把 `Compare changes` 那一整块截图**发给管理员（要能看到四个下拉框的内容）。

---

## 🚀 快速开始（三种方式）

### 方式一：在线编辑（最简单，推荐新手）

适合快速修改文字内容，**全程在浏览器里点，不用装任何软件、也不用开终端**。

> 🤖 **完全没碰过 GitHub 的话**：这条路是最不容易出错的，但页面上的名词（Fork / commit / PR）可能会让你发懵。
> 建议**开着 AI Agent 一起做**，每点到不确定的地方就问一句「我现在在 GitHub 这个页面，
> 下一步该点什么」，见 [配合 AI Agent 一起食用](#-小白强烈建议配合-ai-agent-一起食用)。

#### 步骤：

**第 1 步：打开要编辑的页面**

访问网站 https://www.sldx-ambition.top ，翻到你要改的那个页面。

**第 2 步：点页面底部的「在 GitHub 上编辑此页」按钮**

按钮在正文结束、上一篇/下一篇导航的下方。点它会跳到 GitHub 的在线编辑器。

> 💡 **第一次点会跳到一个 404 页面**，这是正常的：说明你还没有仓库的写入权限，
> GitHub 会让你先 **Fork**（复制一份到你自己的账号）。
> 页面上有个绿色按钮 **`Fork this repository`**，点它，等几秒创建完成，页面会自动跳到编辑器。
> （Fork 只需要做一次，以后点编辑按钮会直接进编辑器。）

**第 3 步：在编辑器里改内容**

GitHub 的在线编辑器左边就是文件内容，直接改就行，支持 Markdown 语法（见下方 [Markdown 基础语法](#-markdown-基础语法)）。

> ⚠️ 在网页编辑器里改**图片是改不了的**（要上传图片请走 [方式二](#方式二使用-vs-code-编辑推荐)），
> 网页编辑只适合**改文字**。

**第 4 步：提交更改（这里怎么选很关键）**

改完后**滚动到页面最底部**，会看到一个 **`Commit changes`** 区域：

1. **上方的小输入框**：填一句修改说明（必填）。
   例：`docs: 更新成员名单联系方式`
2. **下方的两个单选按钮**（默认选中的是下面那个，**要手动改成上面那个**）：
   - ✅ **选这个**：**`Commit directly to the main branch`** —— 直接提交到你自己的 `main` 分支
   - ❌ **不要选**：`Create a new branch for this commit and start a pull request`
3. 点绿色的 **`Commit changes`** 按钮。

> ⚠️ **为什么必须选第一个**：第二个选项会新建一个分支并绕开 `main` 的历史，
> 正是导致「PR 提示差异过大、无法操作」的根源，见 [坑 2](#坑-2不要新建分支直接在-main-上改重要)。

**第 5 步：回到官方仓库发起 Pull Request**

提交完，去官方仓库的 PR 页面：

```
https://github.com/SYLU-Ambition/SLDX_Ambition_Website/pulls
```

1. 点右上角绿色的 **`New pull request`** 按钮；
2. 页面会提示 `Compare changes`，点它右边的蓝色小字 **`compare across forks`**
   （不点这个就只能选官方仓库自己的分支，找不到你的 Fork）；
3. 核对四个下拉框（**从右往左合并**）：

   | 位置 | 选什么 |
   | --- | --- |
   | `base repository` | `SYLU-Ambition/SLDX_Ambition_Website` |
   | `base` | `main` |
   | `head repository` | `你的用户名/SLDX_Ambition_Website` |
   | `compare` | `main` |

4. 下面会列出你改过的文件，**扫一眼确认只有你改的那个文件**；
5. 点绿色的 **`Create pull request`**；
6. 填**标题**（如 `docs: 更新成员名单`）和**说明**，再点一次 **`Create pull request`**。

具体每个环节页面上长什么样、标题说明怎么写，见 [方式二第 8 步 · 在 GitHub 网站上创建 PR](#8-在-github-网站上创建-pull-request逐步图解)（两种方式的 PR 页面完全一样）。

**第 6 步：等审核**

管理员会在 PR 里留评论或直接合并。合并后网站约 1-2 分钟自动更新。
如果管理员让你改，重复第 1-4 步再提交一次即可，**PR 会自动更新，不用重发**。

### 方式二：使用 VS Code 编辑（推荐）

适合大量修改、批量传图，或者改侧边栏这类不能在线编辑的内容。

> 📌 **日常改动（改文字、加照片）全程用鼠标就行**：文件管理器里拖图片、VS Code 里点 `+` 提交。
> 只有「git 连不上网」「想本地预览」「图片特别多要批量压缩」这三种情况才需要开终端，
> 具体见 [速查卡](#-速查卡贴在显示器边上那种)。
>
> 🤖 **建议这个方式一定要配一个 AI Agent 用**：它的环境要求和 Agent 完全一致，
> 装好之后**让 Agent 把「配代理、装依赖、跑 pnpm dev、执行 git 命令」这些事代劳**，
> 你只负责在文件管理器里放图片、在 GitHub 网站上点按钮。见
> [配合 AI Agent 一起食用](#-小白强烈建议配合-ai-agent-一起食用)。

#### 1. 安装必要软件

- **VS Code**：https://code.visualstudio.com/
- **Git**：https://git-scm.com/zh-cn
- 安装 VS Code 插件：`Markdown Preview Enhanced`

#### 2. Fork 仓库

1. 打开 https://github.com/SYLU-Ambition/SLDX_Ambition_Website
2. 点击右上角 **Fork** 按钮
3. 复制你的仓库地址

#### 3. 克隆仓库到本地

> ⚠️ 动手之前请先读 [⚠️ 提交前必读](#-提交前必读新手最容易踩的三个坑)。
> 如果你用的是**系统代理模式**（不是虚拟网卡 / TUN 模式），**先按 [坑 1](#坑-1git-连不上网-clone-卡住不动代理问题) 给 Git 配好代理端口**，否则这一步的 `git clone` 会直接卡死或超时。

**第 1 步：先打开一个终端（命令行）**

- **Windows**：按 `Win` 键 → 输入 `PowerShell` → 回车。或者打开一个文件夹，在地址栏输入 `powershell` 回车（这样会直接在当前文件夹打开终端）。
- **macOS**：`Command + 空格` → 输入 `Terminal` → 回车。
- **Linux**：`Ctrl + Alt + T`。

**第 2 步：检查 Git 装好了没有**

```bash
git --version
# 正常会输出类似：git version 2.43.0
# 如果提示 "command not found" / "不是内部或外部命令"，说明 Git 没装好，回到第 1 步重新安装
```

装完 Git 之后**要把终端关掉重新开一个**，否则新装的东西不生效，这条命令还是会报找不到。

**第 3 步：切到一个用来放代码的文件夹，然后克隆**

```bash
# 选择一个文件夹（不存在的话先建一个，例如 mkdir ~/Projects）
cd ~/Documents   # macOS / Linux
cd D:\Projects   # Windows（注意：换盘符要加 /d，见下方说明）
```

> 💡 **Windows 换盘符的坑**：在 PowerShell 里 `cd D:\Projects` 是可以直接跳过去的；但在 **CMD** 里必须先 `d:` 切换盘符再 `cd D:\Projects`，只用 `cd` 是不会换盘的（这是新手非常常见的困惑）。

```bash
# 克隆你的 fork 仓库（把"你的用户名"换成你 GitHub 的用户名，不要照抄）
git clone https://github.com/你的用户名/SLDX_Ambition_Website.git

# 进入项目目录
cd SLDX_Ambition_Website
```

第一次克隆这个仓库会比较慢（仓库里有大量图片，体积以 GB 计），终端里会显示进度百分比，**耐心等它跑完**。如果卡住不动或报超时，见 [坑 1](#坑-1git-连不上网-clone-卡住不动代理问题)。

**第 4 步：确认自己在 `main` 分支**

```bash
git branch
# 输出里带 * 的那一行应该是 main：
# * main
```

**第 5 步：确认你的 git 身份已经配置过**（只做一次，以后不用再做）

```bash
git config --global user.name    # 看有没有输出
git config --global user.email   # 看有没有输出
```

如果这两条命令**没有任何输出**，第一次 `git commit` 时会直接报错拒绝提交。补上：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的GitHub注册邮箱"
```

#### 4. 编辑内容

用 VS Code 打开项目：菜单 **文件 → 打开文件夹**，选中你 `git clone` 下来的 `SLDX_Ambition_Website` 文件夹。

> 💡 想在终端里一句 `code .` 打开也行（终端里先 `cd` 到项目目录再执行），打不开的话看下面的处理：
> - **macOS**：打开 VS Code → `Cmd+Shift+P` → 输入 `Shell Command: Install 'code' command in PATH` → 回车。
> - **Windows**：重装 Git 时勾选 "Add to PATH"，或者在 VS Code 里 `Ctrl+Shift+P` → 输入同样那句命令。
> - **实在不行**：就用上面「文件 → 打开文件夹」的方式，效果完全一样。

- 所有页面在 `doc/` 目录下，在左侧文件树里展开就能找到
- 编辑 `.md` 文件
- **按 `Ctrl+Shift+V` 预览效果**（macOS 是 `Cmd+Shift+V`）：
  - 不想记快捷键的话，点编辑器**右上角那个「放大镜 + 文档」的小图标**，就是预览按钮。
  - 预览是**分屏**显示的：左边写、右边实时看效果，改一个字右边立刻变。
  - 想看「和网站上几乎一模一样」的效果（带侧边栏、样式、图片懒加载），用 [下面的本地预览](#5-本地预览想看到和网站一样的效果)。
- **不要新建分支，就在 `main` 上改**（见 [坑 2](#坑-2不要新建分支直接在-main-上改重要)）

#### 5. 本地预览（想看到和网站一样的效果）

> 📌 **这一步是可选的。** 只是改文字、加照片的话，不预览直接提交也没问题，管理员审核时会看到效果。
> 但如果你**加了新页面、改了侧边栏、动了图片路径**，强烈建议跑一遍——这些改错会直接白屏或裂图。
> 本地预览需要在**终端**里敲几条命令，也就这一次，跟着复制粘贴即可。

改完内容后，先在本地跑一遍，确认图片能显示、排版没错乱，再推上去。

**前置条件**：装好 Node.js 和 pnpm。

```bash
# 1. 检查 Node.js（需要 18 或更高版本）
node -v
# 输出形如 v22.11.0 才算 OK；如果提示找不到命令，去 https://nodejs.org/ 下载 LTS 版安装

# 2. 启用 pnpm（Node 16.13+ 自带 corepack，一条命令即可启用）
corepack enable
pnpm -v

#    如果上一句报错，改用 npm 全局安装：
#    npm install -g pnpm
```

```bash
# 3. 安装项目依赖（第一次必须做，之后一般不用重复）
pnpm install

# 4. 启动本地预览服务器
pnpm dev
```

终端会输出一个地址，通常是：

```
  ➜  Local:   http://localhost:5173/
```

**按住 Ctrl 点这个链接**（或手动复制到浏览器打开）就能看到本地网站。
改 `.md` 文件后**浏览器会自动刷新**，不用手动重启。

看完之后回到终端，按 **`Ctrl + C`** 停掉服务器（不停掉的话它会一直占着终端）。

> ⚠️ **两个新手常踩的坑**：
> 1. `pnpm install` 报网络错误 → 同样是代理问题，见 [坑 1](#坑-1git-连不上网-clone-卡住不动代理问题)（pnpm 走的是环境变量 `HTTP_PROXY`，不是 `git config`，所以坑 1 里「临时用一次」那段的写法对这里也适用）。
> 2. **不要提交 `node_modules/`**：它是 `pnpm install` 生成的依赖目录，可能有几百 MB。`.gitignore` 已经忽略它了，`git status` 里也不应该出现它。

#### 6. 上传图片的完整操作

**这一步全程用鼠标，在文件管理器里拖拽就行，不需要敲任何命令。**
完整的图库目录说明、各分类该放哪里，见 [🖼️ 上传图片](#-上传图片)。这里以「给 24 对抗赛加照片」为例走一遍全流程。

**第 1 步：在文件管理器里打开项目文件夹**

在 VS Code 左侧项目名上**右键** → **在文件资源管理器中显示**（macOS 是「在访达中显示」）。
或者自己打开你 `git clone` 下来的 `SLDX_Ambition_Website` 文件夹。

**第 2 步：进到目标图库目录**

依次双击 `doc` → `public` → `Photo` → `photo_24dks`。

**第 3 步：看看这个图库已经排到几号了**

把文件夹改成**列表/详细信息**视图（Windows：顶部「查看」→「详细信息」；macOS：`Cmd+2`），
点一下「名称」列排序，拉到最底下。

> ⚠️ **这一步不能省**：重名会**直接覆盖掉旧照片**，网站上只表现为「旧照片怎么变了」，极难排查。
> 假设你看到最大的是 `49.jpg`，那你的新图就从 `50` 开始。

**第 4 步：复制图片进来并改成下一个编号**

选中你的图片 → `Ctrl+C`（macOS `Cmd+C`）→ 回到 `photo_24dks` 文件夹 → `Ctrl+V`（macOS `Cmd+V`）
→ 右键图片 → **重命名** → 改成 `50.jpg`。

> 💡 Windows 看不到扩展名的话：资源管理器顶部「查看」→ 勾选「文件扩展名」，
> **改名时一定要保留 `.jpg` / `.png` 这部分**，只改前面的数字。
> 要一次加好几张，就把它们先批量改成 `50.jpg`、`51.jpg`… 再一起拖进来。

**第 5 步：回到 VS Code，在 `.md` 里加一行引用**

打开 `doc` → `Photo` → `24DKS.md`，在文件**末尾**照着已有格式加一行（方括号里是替代文字，别留空）：

```markdown
![24对抗赛50](/Photo/photo_24dks/50.jpg)
```

**第 6 步：本地预览确认图片显示正常**（这一步不能省）

用 [第 5 步 · 本地预览](#5-本地预览想看到和网站一样的效果) 启动 `pnpm dev`，
打开对应页面**翻到最底部**，确认新加的那张图真的显示出来了（而不是一个裂图图标）。

**第 7 步：在 VS Code 里提交这两个文件**

点左侧 **源代码管理** 图标，你会看到两个文件：

| 文件 | 标记 | 含义 |
| --- | --- | --- |
| `doc/public/Photo/photo_24dks/50.jpg` | **U** | 新增的图片 |
| `doc/Photo/24DKS.md` | **M** | 修改过的页面 |

**两个都要点它右边的 `+` 暂存**，填好说明（如 `docs: 24 对抗赛新增照片`），点 **✓ 提交**，再 **推送**。

> ⚠️ **少提交一个就会出问题**：只提交图片不提交 `.md` → 图上去了但网页上看不到；
> 只提交 `.md` 不提交图片 → 网页上是一片裂图。

> ⚠️ 图片放进去了但网页上不显示？九成是路径写错了：路径必须以 `/` 开头、**不写 `public`**。
> 正确：`/Photo/photo_24dks/50.jpg`；错误：`/public/Photo/...`、`Photo/...`、`../public/Photo/...`。
> 另外**大小写必须和实际文件名完全一致**（服务器是 Linux，区分大小写；本地 Windows 不区分，所以很容易本地正常、线上裂图）。

#### 7. 在 VS Code 中提交并推送

在 VS Code 左侧点击 **源代码管理** 图标（或按 `Ctrl+Shift+G`）。图标长得像「一条线分叉成三条」。

**先看懂这个面板**：文件会分两类显示，这是新手最容易搞混的地方。

| 分区 | 含义 | 要不要管 |
| --- | --- | --- |
| **更改 (Changes)** | 你改了但**还没**告诉 Git 要提交的文件 | 要在这里点 **+** 暂存 |
| **暂存的更改 (Staged Changes)** | 已经点过 **+**、准备提交的文件 | 点 **✓** 提交 |

操作步骤：

1. 把鼠标移到 **更改** 里你要提交的文件上，点击它右边的 **+** 号（暂存更改）。
   文件会从「更改」挪到「暂存的更改」，说明已经准备好了。
   ⚠️ **只点你真正改过的文件**，不要整包全选：
   - `node_modules/`、`dist/`、`doc/public原图备份/`、`doc/wechat-backups/` 这些是本地产物/备份，`.gitignore` 已经忽略它们（正常情况下**根本不会出现在这个列表里**，出现了就说明有问题，先问管理员）。
   - **绝对不要**用 `git add -f` 强行把被忽略的目录加进来。
   - 团队仓库历史上有过「误提交原图备份导致仓库膨胀到 7GB、GitHub 直接拒收推送」的事故，务必引以为戒。
2. 在顶部输入框填写修改说明（如 `docs: 更新成员介绍`），然后点输入框上方的 **✓ 提交** 按钮。
   - 如果弹出「是否要暂存所有更改并提交」的提示，点 **是** 也可以（前提是你确认列表里只有你改的文件）。
   - 如果提示 `Please tell me who you are`，说明 git 身份没配，见 [方式二 · 第 3 步的第 5 点](#3-克隆仓库到本地)。
3. 点 **···**（更多操作）→ **推送 (Push)**，或按 `Ctrl+Shift+P` 输入 `Git: Push` 回车。
   - 推完按钮旁边会出现 **同步更改 (Sync Changes)**，那个是按「拉取 + 推送」，日常用不上，忽略即可。
   ⚠️ 确认推的是 **`main`** 分支（VS Code **左下角状态栏**会显示当前分支名，点一下可以切换分支）。
   如果推送报网络错误，回到 [坑 1](#坑-1git-连不上网-clone-卡住不动代理问题) 配代理。

**VS Code 里看不到「提交」按钮 / 面板是空的？**
点源代码管理面板右上角的 **刷新** 图标；还是空的就说明你真的没改任何文件——先去 `doc/` 里改点东西。

或者使用终端（**可选**，只有你想看清每一步到底发生了什么、或者要截图问人时再用）：

```bash
# 查看你到底改了什么（强烈建议先看一眼再 add）
git status

# 只添加你真正改过的文件（推荐，一次写一个/几个路径）
git add doc/xxx.md doc/public/Photo/xxx.jpg

# 再看一眼，确认「Changes to be committed」里只有你要提交的东西
git status

# 提交更改
git commit -m "docs: 更新了 XX 内容"

# 推送到你自己仓库的 main 分支
git push origin main
```

> 💡 **提交之后想反悔怎么办？**（比如提交信息写错了、多提交了一个文件）——**这些都能用鼠标点，不用敲命令**：
> - **还没提交**（文件还在「更改」区里）：鼠标移到文件上，点右边的 **↩ 撤销更改 (Discard Changes)**。
> - **已经提交但还没推送**：源代码管理面板顶部 **···** → **提交 (Commit)** → **撤销上次提交 (Undo Last Commit)**，
>   改动会退回「更改」区，你可以重新点 `+` 再提交一次。
> - **已经推送了才发现**：**别自己折腾**，在 PR 里说明一下，或者直接问管理员。**不要用 `git push -f`。**

#### 8. 在 GitHub 网站上创建 Pull Request（逐步图解）

推送完成后，**剩下的全部在浏览器里点鼠标完成**，不需要再碰终端。

---

**第 1 步：打开你自己的仓库页面**

地址栏输入（把「你的用户名」换成你自己的 GitHub 用户名）：

```
https://github.com/你的用户名/SLDX_Ambition_Website
```

> 💡 记不住地址的话：登录 GitHub → 右上角**头像** → **Your repositories** → 点进 `SLDX_Ambition_Website`。

**第 2 步：找到发起 PR 的入口**

刚推送完的话，仓库首页**顶部会弹出一条黄色横幅**：

```
你的分支比 SYLU-Ambition:main 领先 1 个提交。
                                     [ Compare & pull request ]
```

**直接点那个绿色的 `Compare & pull request` 按钮**，跳到第 3 步。

> 没看到这条横幅也没关系，用下面任意一种方式：
> - 点仓库顶部菜单栏的 **Pull requests**（图标是一个分叉箭头）→ 点右边的绿色按钮 **New pull request**；
> - 直接访问 `https://github.com/SYLU-Ambition/SLDX_Ambition_Website/compare`。

**第 3 步：确认「谁合并到谁」（这一步最容易选错，务必核对）**

页面顶部有一排下拉框和箭头，写成 `base ← compare` 的形式，**箭头方向是「从右往左合并」**：

```
base repository: SYLU-Ambition/SLDX_Ambition_Website   base: main   ←   head repository: 你的用户名/SLDX_Ambition_Website   compare: main
```

对着这张表逐项核对：

| 位置 | 应该选什么 | 说明 |
| --- | --- | --- |
| 左边 `base repository` | **`SYLU-Ambition/SLDX_Ambition_Website`** | 官方仓库，你要合并**进**去的地方 |
| 左边 `base` | **`main`** | 官方仓库的主分支 |
| 右边 `head repository` | **`你的用户名/SLDX_Ambition_Website`** | 你自己的 Fork |
| 右边 `compare` | **`main`** | 你改动的分支 |

- 如果右边默认是你的某个**别的分支名**（不是 `main`），点下拉框改成 `main`——改完页面会重新加载差异。
- 如果页面上写着 **`There isn't anything to compare.`**，说明选错了或者你在分支上操作过，见 [坑 3](#坑-3找不到-compare-pull-request-按钮-提示没有可比较的内容)。

**第 4 步：核对下面的文件差异列表**

下拉框下方的 **`Files changed` 标签页**会列出你改动过的每个文件，**先花 10 秒扫一眼**：

- ✅ **应该看到**：绿色的 `+` 行是你新加的内容，红色的 `-` 行是你删掉的内容；文件数量和你预期一致。
- ❌ **不该看到**：`node_modules/`、`dist/`、`doc/public原图备份/`、几十个你没碰过的 `.jpg` 显示为"修改"、
  或者显示 **几千个文件变更**。

> ⚠️ 如果差异明显不对，**先别点创建**。关掉这个页面，回 VS Code 检查是不是误提交了东西，
> 或者直接找管理员看一眼。差异正常再继续。

**第 5 步：点绿色的 `Create pull request` 按钮**

（按钮在页面右上角。点了以后才会进入写标题和说明的页面。）

**第 6 步：填写标题和说明**

页面出现一个表单，两个输入框：

- **标题框**（短，一行）：写清楚这次改了什么。
  - 好例子：`docs: 更新 24 赛季对抗赛照片`、`fix: 修正成员名单里的联系方式`
  - 坏例子：`更新`、`修改`、`1`（管理员完全看不出你要干嘛）
  - 和历史提交风格保持一致，中文即可，`docs:` / `fix:` / `feat:` 是前缀惯例。
- **说明框**（大，支持 Markdown）：建议写三段，可以直接复制下面这个模板改：

```markdown
## 改了什么
- 在 doc/Photo/24DKS.md 新增 5 张照片
- 对应图片放在 doc/public/Photo/photo_24dks/50-54.jpg

## 为什么改
这批照片是 24 赛季分区赛现场拍的，之前一直没上传。

## 我怎么验证的
本地 pnpm dev 跑过，5 张图都能正常显示，手机宽度下排版也正常。
```

- 不想写这么细的话，**至少写清楚「改了哪个页面、改了什么」**。
- 如果这个 PR 关联了某个 Issue（比如第 12 号），在说明里加一行 `Closes #12`，合并时会自动关闭它。

**第 7 步：再次点绿色的 `Create pull request` 按钮提交**

（对，又是这个按钮，这次在表单右下角。）点完页面会跳转到你刚创建的 PR 页面。

**第 8 步：等审核 / 盯进度**

PR 页面从上到下依次是：

| 区域 | 看什么 |
| --- | --- |
| 最上方标题栏 | 状态标记：**Open**（待审核）→ **Merged**（已合并，紫色） |
| 中间的 **Conversation** 标签 | 管理员的评论和修改意见都在这；有新回复会发邮件到你 GitHub 邮箱 |
| 下面的 **Files changed** 标签 | 你这次改动的完整差异 |
| 页面底部 | `Close pull request`（自己关闭）、合并按钮（**只有管理员能点**） |

- **管理员让你改怎么办？**
  不用重新发 PR、不用新建分支。**直接在本地 `main` 上继续改**，改完照原来的方式再提交推送一次
  （VS Code 源代码管理 → 点 `+` → 填说明 → **✓ 提交** → **推送**）。
  **PR 会自动更新**，新提交会直接出现在原来这个 PR 的 Conversation 里。
  改完在评论区回复一句「已按意见修改」即可。
- **合并之后**：网站约 1-2 分钟自动更新。
  想看部署进度，点仓库顶部菜单的 **Actions** 标签页，最新那条 workflow 变成**绿色对勾**就说明上线了；
  如果是**红色叉**，说明构建失败，截图发给管理员。

**第 9 步：把官方仓库的最新改动同步回你的 Fork**（合并后做一次）

你自己的 Fork 这时会落后于官方仓库。到你自己仓库首页，点 **`Sync fork`** → **`Update branch`**，
把官方 `main` 同步到你的 `main`，这样下次改动不会冲突。

> 如果这一步提示有冲突（`Discard N commits` 之类），**别乱点**，截图问管理员。

> 如果没看到 `Compare & pull request` 按钮，或者页面提示差异异常巨大，见 [坑 3](#坑-3找不到-compare-pull-request-按钮-提示没有可比较的内容)。



## 📝 Markdown 基础语法

### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 文字格式

```markdown
**加粗**  *斜体*  ~~删除线~~
```

### 列表

```markdown
- 无序列表 1
- 无序列表 2

1. 有序列表 1
2. 有序列表 2
```

### 链接

```markdown
[文字](https://链接地址)
```

### 图片

```markdown
![图片说明](/images/photo.jpg)
```

### 代码块

````markdown
```python
print("Hello World")
```
````

### 引用

```markdown
> 这是一段引用
```

### 表格

```markdown
| 姓名 | 年龄 | 组别 |
|------|------|------|
| 张三 | 20   | 嵌入式 |
| 李四 | 21   | 视觉  |
```

---

## 🔄 Pull Request (PR) 完整流程

### 什么是 Pull Request？

Pull Request（简称 PR）是你向项目提出"我想修改这些内容，请审核并合并"的请求。

**用一句话理解**：你不能直接改官方仓库，所以你先 **Fork**（复制一份到自己账号）→ 在自己的副本里改 →
再通过 PR 告诉官方仓库「我在副本里改好了，你看看要不要收下」。管理员审核通过后点合并，你的改动就进官网了。

### 整体流程图

```
① Fork 仓库      → 复制一份到你自己账号（只需做一次）
② 改内容          → 网页在线编辑，或在本地 VS Code 里改（在 main 分支上！）
③ 提交            → 网页点 Commit changes，或本地点 ✓ 提交 + 推送
④ 发起 PR         → 在 GitHub 网站上点 Compare & pull request
⑤ 填写标题和说明   → 告诉管理员你改了什么、为什么改
⑥ 等待审核        → 管理员审查，可能让你再改（改完 PR 自动更新）
⑦ 管理员点合并     → 网站约 1-2 分钟自动更新上线
```

**其中第 ④~⑦ 步全部在 GitHub 网站上点鼠标完成**，逐步图解见
[方式二第 8 步 · 在 GitHub 网站上创建 Pull Request](#8-在-github-网站上创建-pull-request逐步图解)。

### PR 页面各区域是干嘛的

第一次打开 PR 页面容易懵，对照这张表看：

| 位置 | 名称 | 作用 |
| --- | --- | --- |
| 最上方 | 标题 + 状态标记 | **Open**=待审核 / **Merged**(紫色)=已合并 / **Closed**(红色)=已关闭 |
| 标题下方 | 绿色 `Merge pull request` 按钮 | **只有管理员能点**，你这里点了没用 |
| 中间大区域 | **Conversation** 标签 | 讨论区：管理员在这里留评论和修改意见，你在这里回复 |
| 中间大区域 | **Commits** 标签 | 这个 PR 里包含的提交列表 |
| 中间大区域 | **Checks** 标签 | 自动构建结果，绿色对勾=通过，红色叉=构建失败 |
| 中间大区域 | **Files changed** 标签 | 你改动的**逐行差异**：绿色 `+` 是新增，红色 `-` 是删除 |
| 页面底部 | `Close pull request` | 自己关掉这个 PR（不想要了就用它，不用删仓库） |

> 💡 **管理员让你改的时候**：**不要**新建 PR，也**不要**新建分支。
> 直接在原来的地方改好，再提交推送一次，新改动会**自动追加到这个 PR 里**。
> 改完在 Conversation 里回复一句「已按意见修改」即可。

### PR 提交前的自检清单

发起 PR 之前，对照着过一遍：

- [ ] 我的改动提交在**我自己 Fork 的 `main`** 分支上（不是别的分支名）
- [ ] 没有新建过分支（没有用过 `git checkout -b` / `Create a new branch for this commit`）
- [ ] 没有把 `node_modules/`、`dist/`、`doc/public原图备份/`、`doc/wechat-backups/` 提交上去
- [ ] 修改说明写清楚了改了什么（如 `docs: 更新 2024 赛季合照`）
- [ ] PR 的 **Files changed** 里只看到我这次改的文件，没有异常的大量差异
- [ ] base 是 `SYLU-Ambition/SLDX_Ambition_Website:main`，compare 是 `你的用户名/...:main`

### PR 被拒绝怎么办？

- 管理员可能会提出修改建议（这叫 review）
- 根据评论修改后**再提交推送一次**即可，PR 会自动更新，**不需要重发**
- 这是正常的协作流程，不要担心，被要求修改不代表你做错了什么

### PR 里混进了不该有的东西 / 差异特别大怎么办？

- **不要**自己删库重来。先在 PR 页面底部点 **`Close pull request`** 关掉这个 PR，然后找管理员说明情况。
- 差异特别大，几乎都是**在分支上操作**导致的，参照 [坑 2](#坑-2不要新建分支直接在-main-上改重要) 和
  [坑 3](#坑-3找不到-compare-pull-request-按钮-提示没有可比较的内容) 处理。

---

## 📁 文件结构说明

```
SLDX_Ambition_Website/
├── doc/                        # ★ 所有页面内容都在这里（VitePress 的 srcDir）
│   ├── index.md                # 首页
│   ├── contents.md             # 快速导航页
│   ├── team.md                 # 官网项目组（成员卡片页）
│   ├── wechat.md               # 公众号文章目录页
│   ├── wechat.data.js          # 公众号文章的「数据源」，自动扫描文章目录
│   ├── Ambition_Introduction/  # 战队介绍（赛事介绍/战队简介/战绩/合照/成员/兵种图/宣传视频）
│   ├── History/                # 战队历史（history1.md 发展历程）
│   ├── Sign_Up/                # 招新（招新介绍/组别介绍/Q&A）
│   ├── Photo/                  # 照片展示（每个赛事一个 .md，如 24DKS.md）
│   ├── Knowledge/              # 知识库（SW 安装指南、RM 操作手端使用）
│   ├── public/                 # ★ 图片等静态资源（见下方说明）
│   │   ├── Photo/              #   照片图库，每期一个子目录 photo_24dks/
│   │   ├── Ambition_Introduction/
│   │   ├── History/
│   │   ├── Knowledge/
│   │   └── wechat/articles/    #   公众号文章存档（外部工具生成）
│   ├── public原图备份/          # ⛔ 本地原图备份，禁止提交（已被 gitignore）
│   └── wechat-backups/         # ⛔ 公众号原图备份，禁止提交（已被 gitignore）
├── .vitepress/                 # 网站配置（一般不用动）
│   ├── config.mjs              #   主配置：站点标题、srcDir、主题、图片插件
│   ├── nav.mjs                 #   页眉导航按钮
│   ├── sidebar.mjs             #   ★ 左侧边栏目录（加新页面要改这里）
│   ├── socialLinks.mjs         #   页眉社交图标
│   └── theme/                  #   主题与自定义组件
│       └── components/
│           └── EditOnGitHub.vue  # 页面上那个「在 GitHub 上编辑此页」按钮
├── scripts/
│   └── optimize_wechat.py      # 图片压缩 / CSS 去重 / HTML 压缩 脚本
├── .github/workflows/          # GitHub Actions：推送到 main 后自动构建部署
├── AGENTS.md                   # 给 AI 助手看的项目约定（人类也可以看，有干货）
├── 编辑指南.md                  # ← 你正在看的这份文档
└── package.json                # 项目依赖和命令（pnpm dev / build）
```

**两条最容易搞错的路径规则**：

1. **`doc/public/` 是「静态资源根目录」**。放在 `doc/public/Photo/24DKS/01.jpg` 的图，
   在 Markdown 里要写成 `/Photo/24DKS/01.jpg`——**去掉 `doc/public` 前缀**。
2. **`.md` 文件在 `doc/` 下**，但侧边栏里的 `link` 是**相对 `doc/` 的、不带 `.md`** 的路径：
   文件 `doc/History/history1.md` → `link: '/History/history1'`。

> 📌 **`doc/` 目录下不要放除页面和 `public/` 之外的东西**（比如别把原图、视频、压缩包丢在 `doc/` 根下），
> 否则会被当成页面源文件参与构建，甚至被打进站点产物里。

---

## 📄 一些页面的特殊写法

这几个页面结构比较特殊，改之前先看一眼，别按普通 `.md` 的方式硬改。

### `doc/team.md` —— 成员卡片页

用的是 VitePress 自带的团队组件，成员信息是**写在 `<script setup>` 里的 JavaScript 数组**，不是普通的 Markdown 正文：

```javascript
const members = [
  {//名单顺序以加入项目组时间为准
    avatar: 'https://github.com/zhanwuzhijing.png',   // 头像：直接用 GitHub 头像地址最方便
    name: '战空',
    title: '23-25赛季运营组 · 项目负责人',
    desc: 'QQ：2276539496',
    links: [
      { icon: 'github', link: 'https://github.com/zhanwuzhijing' },
    ]
  },
]
```

**要加人**：照着上面这一份**复制一份**（包括最外层那对 `{ }`），改 `avatar` / `name` / `title` / `desc` / `links` 就行。

- 数组每一项之间要有**英文逗号**；
- `icon` 只支持 VitePress 内置图标（`github`、`twitter`、`discord` 等），想放 B 站/QQ 这种自定义图标要写一长串 `svg:` 代码——**这一步建议直接找管理员**，别自己拼；
- 改完 `pnpm dev` 看一下头像有没有正常加载（头像地址填错会很直观地显示成裂图）。

### `doc/Photo/*.md` —— 照片墙

结构极简，就是**一行一张图的 Markdown 图片语法**，没有别的：

```markdown
# 24赛季对抗赛·分区赛·东部赛区·苏州
![24对抗赛01](/Photo/photo_24dks/01.jpg)
![24对抗赛02](/Photo/photo_24dks/02.jpg)
```

**要加照片**：在文件末尾照着现有格式加新行即可。

- 方括号里的是**替代文字**（图片加载失败时显示，也影响 SEO），**别留空**；
- 圆括号里的路径必须和实际文件**大小写完全一致**（Linux 服务器区分大小写，本地 Windows 不区分，很容易本地正常、线上裂图）；
- 加完记得 `pnpm dev` 翻到页面底部，确认最后几张图确实加载出来了。

### `doc/wechat.md` + `doc/wechat.data.js` —— 公众号文章存档

这一块是**自动化**的，不是手写的：

```
外部归档工具产出 HTML
   ↓ 放进 doc/public/wechat/articles/<日期>_<标题>/
   ↓ 运行 scripts/optimize_wechat.py（优化图片/CSS、生成元数据）
   ↓ wechat.data.js 自动扫描目录并读取 wechat-metadata.json
   ↓ wechat.md 渲染出文章目录页
```

**一般不用手动改这个页面**。如果某篇文章不想让它出现在目录里：

1. 打开 `doc/wechat.data.js`，找到 `SKIP_ARTICLES` 数组，把**文章目录名**加进去：
   ```javascript
   const SKIP_ARTICLES = [
     '2026-05-22_公众号运营回归个人通知',
     '2025-09-15_我们搬家啦',
     // 在这后面加一行，注意保留英文逗号：
     '2025-01-01_某篇不想展示的文章',
   ]
   ```
2. **同时**在 `scripts/optimize_wechat.py` 里找到同名的 `SKIP` 数组，加一份一样的。
   > ⚠️ **两个文件必须同步改**，只改一个会出现「目录页看不到、但构建产物里还有」或者反过来的不一致。
   > 这一步属于**管理员操作**，普通成员发现某篇文章想下架，直接找管理员说一声就行。
3. 由管理员重新跑一遍脚本并构建（`python3 scripts/optimize_wechat.py` + `pnpm run build`）。
4. 另外，**标题以「转载」开头的文章会被自动排除**，不用手动加进 SKIP。

> ⚠️ 公众号文章目录下的大 `mp4` 视频**不纳入 git 管理**（已在 `.gitignore` 里），
> 如果你发现某个视频没上传，这不是坏了，是故意的——仓库放不下。

---

## 🖼️ 上传图片

网站的图片统一放在 `doc/public/` 目录下（VitePress 会把 `doc/` 下的 `public/` 当作静态资源根目录），在 Markdown 中用**以 `/` 开头的绝对路径**引用。

> 💡 **上传图片全程不需要敲命令行**，用系统的文件管理器（Windows 资源管理器 / macOS 访达）拖拽即可。
> 下面第 3 节「🖱️ 在文件管理器里放图片」是完整的鼠标操作版，照做就行。

### 🖱️ 在文件管理器里放图片（主推方式）

**第 1 步：打开项目文件夹**

- **Windows**：在 VS Code 里右键左侧项目名 → **在文件资源管理器中显示**（`Reveal in File Explorer`）。
- **macOS**：右键项目名 → **在访达中显示**（`Reveal in Finder`）。
- 或者直接打开资源管理器/访达，进到你 `git clone` 下来的那个 `SLDX_Ambition_Website` 文件夹。

**第 2 步：一层一层进到目标图库目录**

依次双击进入 `doc` → `public` → `Photo` → 找到对应那期的文件夹（如 `photo_24dks`）。

| 你要放的图片类型 | 该去哪个文件夹（在 `doc/public/` 下面找） |
| --- | --- |
| 比赛照片 | `Photo/photo_24dks/`（24 对抗赛，其他期同理 `photo_23dks`、`photo_25dks`…） |
| 战队介绍相关图 | `Ambition_Introduction/` 下对应子目录，如 `robot_photo/` |
| 历史相关图 | `History/` |
| 知识库图 | `Knowledge/` |

**第 3 步：看一下这个文件夹里已经排到几号了**

把文件夹的**查看方式改成「详细信息 / 列表」**（Windows：顶部「查看」→「详细信息」；macOS：访达里按 `Cmd+2`），
然后按**名称排序**（点一下「名称」那一列的表头），拉到最底下看最大编号。

> ⚠️ **这一步不能省**：如果新图片取了和已有图片一样的名字，**会直接覆盖掉旧照片**，
> 而且网站上只会表现为「旧照片怎么变了」，非常难排查。

**第 4 步：把图片复制进这个文件夹，并改成下一个编号**

1. 在你的下载目录里选中图片 → `Ctrl+C`（macOS `Cmd+C`）
2. 回到刚才打开的图库文件夹 → `Ctrl+V`（macOS `Cmd+V`）
3. **右键图片 → 重命名**，改成下一个编号。假设刚才看到最大是 `49`，那这张就叫 `50.jpg`
   - **扩展名（`.jpg` / `.png`）必须保留**，只改前面的名字。
   - Windows 如果看不到扩展名：资源管理器顶部「查看」→ 勾选「文件扩展名」。
   - 如果一次要放好几张，建议先批量改好名（`50.jpg`、`51.jpg`…）再一起粘贴，省得一张张改。

**第 5 步：在编辑器里给这个 `.md` 加一行引用**

回到 VS Code，打开 `doc` → `Photo` → `24DKS.md`（和图片文件夹同名的那个），在**文件末尾**照着已有格式加一行：

```markdown
![24对抗赛50](/Photo/photo_24dks/50.jpg)
```

- 方括号里是**替代文字**，别留空（图片没加载出来时会显示这句话，也影响搜索引擎收录）。
- 圆括号里的路径 = `/` + 从 `public` 里面开始算的路径，**不要写 `public` 这三个字**。

**第 6 步：本地预览确认图片真的出来了**

用 [第 5 步 · 本地预览](#5-本地预览想看到和网站一样的效果) 启动 `pnpm dev`，打开对应页面**翻到最底部**，
确认新加的图**显示出来了**而不是一个裂图图标。

**第 7 步：提交（图片和 `.md` 两个文件都要提交）**

回到 VS Code 的**源代码管理**面板，你会看到两个文件：

- `doc/public/Photo/photo_24dks/50.jpg` → 标记为 **U**（新文件）
- `doc/Photo/24DKS.md` → 标记为 **M**（修改）

**两个都要点 `+` 暂存**，然后填说明、提交、推送（见 [第 7 步 · 在 VS Code 中提交并推送](#7-在-vs-code-中提交并推送)）。

> ⚠️ **只提交图片不提交 `.md`** → 图上去了但网页上看不到；
> **只提交 `.md` 不提交图片** → 网页上是一片裂图。

### 步骤（简版）

1. 把图片放到 `doc/public/` 下对应的分类目录，例如：
   - 照片图库：`doc/public/Photo/`
   - 战队介绍相关图：`doc/public/Ambition_Introduction/`
   - 历史：`doc/public/History/`
   - 知识库：`doc/public/Knowledge/`

2. 在 Markdown 中引用（注意路径以 `/` 开头，不写 `public`）：
   ```markdown
   ![图片说明](/Photo/photo_23dks/01.jpg)
   ![机器人图](/Ambition_Introduction/robot_photo/19BB.png)
   ```

### 📏 图片处理建议（重要）

**1. 图片必须压缩后再上传**
- 单张图片建议控制在 **1MB 以内**，优先用 `JPG`（照片）或 `WebP`（更小体积）。
- 相机/手机拍的原图动辄几 MB，**必须压缩后再放进 `doc/public/`**，不要直接传原图上来。
- 可用在线工具或本地命令压缩（如 `Squoosh`、`ImageMagick`、`TinyPNG`、`pngquant`）。

**2. 千万不要把「原图备份」这类大目录提交进仓库**
- `doc/public原图备份/`（及任何本地备份/网盘同步目录）**只保留在本地，不要 commit**。
- `.gitignore` 已经忽略了它们，**不要用 `git add -f` 强行加进去**。
- 这是血的教训：之前这个备份目录被误提交进历史，导致仓库膨胀到 7GB+，GitHub 直接拒绝接收推送。大目录一旦进历史，很难干净移除，还会把整个仓库拖垮。

**3. 图片命名**
- 用英文或拼音命名，不要用中文和空格。
- 建议带上分类和期数，容易管理，如 `photo_23dks_01.jpg`。
- 有了现成图库目录（如 `doc/public/Photo/photo_24dks/`）就**接着现有编号往后排**（`01.jpg`、`02.jpg`…），不要跳号、不要重名——重名会**直接覆盖掉旧图**，而且 Git 里还能恢复，网站上看就是「旧照片莫名变了」。

**4. 图片引用路径要写对**
- 路径以 `/` 开头，**不要**写成 `public/...` 或 `../public/...`。
- **大小写必须和实际文件名完全一致**：服务器是 Linux，区分大小写；本地 Windows 不区分。
  所以 `50.JPG` 和 `50.jpg`、`photo_24dks` 和 `photo_24DKS` 在本地看着都对，传上去就是裂图。
- 引用前先 `pnpm dev` 预览确认图片能正常显示。

### 🔧 怎么压缩图片（照着做）

压缩是**上传前在你自己电脑上做**的，跟这个仓库没关系，用鼠标操作就行。

**方法 A：在线工具（推荐，最省事，不用装任何东西）**

- [Squoosh](https://squoosh.app/)（Google 出品，网页版，能左右拖动实时对比压缩前后的画质）
- [TinyPNG](https://tinypng.com/)（把图拖进网页就自动压，免注册）

用法一模一样：

1. 浏览器打开上面任意一个网址；
2. 把图片**拖进网页**；
3. 等它压完，点 **Download** 下载；
4. 用下载回来的文件**覆盖**你原本要放进去的那张图（重新命名成 `50.jpg` 再放进图库文件夹）。

> 💡 TinyPNG 支持一次拖**多张**进去批量压；Squoosh 更强但一次一张。照片多的话可以先用 TinyPNG 过一遍。

**方法 B：用系统自带/常用看图软件（不用联网）**

- **macOS（最方便）**：用系统自带的**预览**打开图片 → 菜单 **文件 → 导出** → 格式选 **JPEG** →
  把**质量滑杆拉到中间偏左**（约 70~80%）→ 保存。系统自带，一行命令都不用敲。
- **Windows**：装个 [Honeyview](https://www.bandisoft.com/honeyview/) 或 [XnView MP](https://www.xnview.com/en/xnviewmp/)，
  打开图片后在菜单里找「另存为 / 转换」，把质量调到 75~80 再保存。

**压完怎么算合格**

不用追求精确数字，按这个标准就够：

- **单张控制在 1MB 以内**，最好在 300KB 上下；
- 在浏览器里把图片缩到**手机屏幕大小**看，没有明显糊掉、没有马赛克块，就算合格。

> 🔧 **进阶：用脚本批量压缩（可选，普通成员用不上）**
>
> 仓库里的 `scripts/optimize_wechat.py` 会把 `doc/public/` 下的图片统一压到宽 1200px、JPEG 质量 75，
> 适合**一次新增几十上百张图**的情况。需要装 Python 和 Pillow：
>
> ```bash
> pip install Pillow
> python3 scripts/optimize_wechat.py
> ```
>
> ⚠️ **用之前必须知道它的副作用**（项目 `AGENTS.md` 里有记录）：
> 它会**遍历整个目录**批量重压，对**已经压过的图片**重复运行会产生**字节级漂移**——
> 表现为 VS Code 源代码管理里一大堆 `.jpg` 显示 **M**，但内容其实没变、毫无意义。
> **这类改动不要提交**，丢弃即可：
>
> ```bash
> git restore doc/public/Photo/
> ```
>
> 结论：**只有确实新增了图片时才跑它，跑完只提交你新增的那几张。**
> 不确定要不要跑就在群里问一句管理员。

**最后确认效果**：`pnpm dev` 打开对应页面看排版有没有乱；也可以按 `F12` 打开开发者工具 → **Network** 面板
刷新页面，看图片的实际传输大小，几百 KB 就是正常的。

### 注意：
- 团队合照建议说明时间、事件
- 大图片不仅影响加载速度，还会让仓库越滚越大，请务必压缩

---

## 📋 添加新页面

新增一个页面需要**改两个文件**：先建内容文件，再去侧边栏登记，缺一个页面上就找不到入口。

### 步骤 1：创建 `.md` 文件

在 `doc/` 下对应的分类目录里新建 `.md` 文件，例如 `doc/History/history2.md`。

**文件名的坑**：
- 用**英文/拼音**，别用中文和空格（文件名会成为网址的一部分，中文会被转义成一长串 `%E5%8E%86%E5%8F%B2`，很难看也不好分享）。
- 全小写，用下划线或短横线分词：`team_photos.md`、`sign-up-faq.md`。

### 步骤 2：写页面内容（直接用这个模板）

```markdown
---
title: 页面标题
description: 页面描述，会显示在搜索结果里
---

# 页面标题

正文内容...

## 小节标题

更多内容...
```

`---` 包起来的那一块叫 **frontmatter**，是可选的，但**加上 `title` 和 `description` 对 SEO（百度/谷歌收录）有好处**，建议都写上。

> ⚠️ **本项目特有的一条坑**：VitePress 的 Markdown 里**不要用 `<template>` 标签**，需要类似效果请用 `<div>` 代替。
> 原因见项目 `AGENTS.md`：`<template>` 会被 Vue 当成组件模板解析，导致内容不显示。

### 步骤 3：在侧边栏登记（这一步不做，页面上就没有入口）

打开 `.vitepress/sidebar.mjs`，找到对应的分类，在 `items` 数组里加一行：

```javascript
{
  text: '要加的分类名',
  collapsed: true,          // true = 默认折叠，false = 默认展开
  items: [
    { text: '发展历程', link: '/History/history1' },
    { text: '续写新篇', link: '/History/history2' },   // ← 你新加的这行
  ]
},
```

**几个必须注意的格式问题**（JavaScript 语法一错，`pnpm dev` 直接白屏报错）：

- `link` 的路径**以 `/` 开头**、**不写 `doc/`、不写 `.md` 后缀**。
  正确：`/History/history2`；错误：`/doc/History/history2.md`、`History/history2.md`。
- 每一项之间用 **英文逗号** `,` 分隔，别漏也别用中文逗号 `，`。
- `text` 和 `link` 用**单引号** `'...'` 包起来。
- 侧边栏的分类是**手写顺序**的，新加的项目想排在哪里，就插在哪一行后面。

### 步骤 4：导航栏（页眉按钮）要不要也加？

`.vitepress/nav.mjs` 是**页眉**那排按钮，结构比侧边栏简单（没有 `collapsed`）。

- **一般不用改**：绝大多数新页面只需要出现在左侧边栏就够了。
- **只有当你新增了一整个大分类**（比如「赛事资源」这种一级入口）时，才需要管理员在 `nav.mjs` 里也加一份。

> 💡 所以：**新增普通页面 → 只改 `sidebar.mjs`**；**新增一级大分类 → `nav.mjs` 和 `sidebar.mjs` 都要改**（建议这一步找管理员一起做）。

### 步骤 5：本地验证

**这一步必须做**——新建页面 + 改侧边栏是最容易白屏的操作（见 [方式二第 5 步](#5-本地预览想看到和网站一样的效果) 启动本地预览）：

```bash
pnpm dev
```

然后**必须检查这四件事**：

1. 左侧边栏能看到你新加的那一项，点击能跳转；
2. 页面标题、正文显示正常，没有「白屏」或大段内容消失；
3. 图片都能正常显示（如果你加图了）；
4. 浏览器 `F12` 的 **Console** 面板里没有红色报错。

四条都过了，回到 VS Code 的**源代码管理**面板，确认**两个文件都在**：

| 文件 | 标记 |
| --- | --- |
| `doc/History/history2.md` | **U**（新增） |
| `.vitepress/sidebar.mjs` | **M**（修改） |

两个都点 `+` 暂存 → 填说明 → **✓ 提交** → **推送**。

> ⚠️ `.vitepress` 是**开头带点的隐藏文件夹**，在 VS Code 文件树里能直接看到，但 Windows 资源管理器里默认不显示，
> 所以这一步建议在 VS Code 里做，别去资源管理器找。
> 忘了改 `sidebar.mjs` 的后果是：页面上线了但**侧边栏没有入口**，别人完全找不到。

> ⚠️ 加新页面时**很容易忘了 `git add .vitepress/sidebar.mjs`**，结果页面上线了但侧边栏没入口，
> 别人完全找不到。提交前用 `git status` 核对一遍，两个文件都要在里面。

---

## ❓ 常见问题

> 🤖 **这里的条目没覆盖你的问题？** 直接把**报错原文 + 你的操作**丢给 AI Agent 问，
> 见 [配合 AI Agent 一起食用](#-小白强烈建议配合-ai-agent-一起食用)。
> 下面这些问答你也可以整段复制给 AI，让它结合你的实际情况判断。

### Q: 我完全不懂命令行，能改这个网站吗？

A: **能。** 日常改动（改文字、加照片）**全程用鼠标**就行——在文件管理器里拖图片、
在 VS Code 里点 `+` 提交、在 GitHub 网站上点按钮发 PR，见 [速查卡](#-速查卡贴在显示器边上那种)。

只有三种情况需要开终端：git 连不上网、想本地预览、图片特别多要批量压缩。
**这三种情况都可以让 AI Agent 代劳**——把报错贴给它就行，见
[小白强烈建议：配合 AI Agent 一起食用](#-小白强烈建议配合-ai-agent-一起食用)。

### Q: 修改后网站为什么没有更新？

A: 需要等待管理员审核并合并 Pull Request，合并后约 1-2 分钟自动更新。
想看部署进度，去官方仓库的 **Actions** 标签页，最新一条变成绿色对勾就是上线了。

### Q: `git clone` / `git push` 卡住不动或者报连接超时？

A: 99% 是代理问题。详见 [坑 1](#坑-1git-连不上网-clone-卡住不动代理问题)。
一句话：**系统代理模式必须给 Git 手动指定端口（Clash 默认 `7897`），虚拟网卡 / TUN 模式不用。**

### Q: 我应该新建一个分支再提 PR 吗？

A: **不应该。就在你自己的 `main` 分支上改、提交、推送。**
建分支（尤其是空白分支）会让 PR 出现「差异过大 / 无法比较」的问题，详见
[坑 2](#坑-2不要新建分支直接在-main-上改重要)。

### Q: 上游仓库更新了，我的 Fork 落后了怎么办？

A: 打开**你自己 Fork 的仓库首页**（`https://github.com/你的用户名/SLDX_Ambition_Website`），
在文件列表上方有一行提示 `This branch is N commits behind SYLU-Ambition:main.`，
右边有一个 **`Sync fork`** 按钮：

1. 点 **`Sync fork`**，弹出一个小菜单；
2. 点绿色的 **`Update branch`**；
3. 等一下，提示变成 `This branch is up to date` 就同步好了。

> ⚠️ 如果菜单里显示的是红色的 **`Discard N commits`** 而不是 `Update branch`，
> 说明你 Fork 的 `main` 上有本地提交和上游冲突了，**别点**（点了会丢掉你的提交），截图问管理员。
>
> 如果本地也要跟着更新：在 VS Code 源代码管理面板点 **···** → **拉取 (Pull)** 即可
> （前提是你本地改动已经提交，否则可能冲突）。

### Q: 如何查看我的 PR 状态？

A: 两种查法：

- **看某一条**：进 PR 页面，最上方的状态标记就是结论——
  **Open**（绿，等审核）、**Merged**（紫，已合并上线）、**Closed**（红，被关闭）。
- **看全部**：
  - 你自己仓库：`https://github.com/你的用户名/SLDX_Ambition_Website/pulls`（看你自己发起的）；
  - 官方仓库：`https://github.com/SYLU-Ambition/SLDX_Ambition_Website/pulls`（看所有人在提的）。

### Q: 多人同时修改会冲突吗？

A: 如果修改**不同文件**不会冲突。如果**改同一个文件的同一段**，后提交的人需要解决冲突。

- GitHub 会在 PR 页面显示 **`This branch has conflicts that must be resolved`**。
- 遇到冲突**不要自己瞎改**，在 PR 评论区 @ 一下管理员，让管理员协调处理。
- 想尽量避开冲突：开始改之前先 `Sync fork` 同步一次最新版（见上面第一条），别在很旧的版本上改。

### Q: 如何撤销已提交的 PR？

A: 在你的 PR 页面**拉到最底部**，点 **`Close pull request`** 即可关闭。
关闭**不会**删掉你的改动，只是这个 PR 不再等待合并；需要时可以重新发起。
如果 PR 已经被合并了才发现有问题，**找管理员**（合并后的回退需要走新的 PR）。

### Q: 可以在本地预览效果吗？

A: 可以，需要**开终端**装一次 Node.js 和 pnpm，之后每次预览只要两条命令（完整说明见 [方式二第 5 步](#5-本地预览想看到和网站一样的效果)）：

```bash
pnpm install     # 只有第一次需要
pnpm dev         # 看完按 Ctrl+C 停掉
```

> 如果你只是改了文字或加了照片，**不预览也可以直接提交**，管理员审核时会看到实际效果。
> 但新页面、侧边栏、图片路径这三类改动**一定要预览**，否则很容易白屏或裂图。

### Q: VS Code 里推送报错，但我不确定是不是代理问题？

A: 在 VS Code 里打开终端（`` Ctrl+` ``），手动执行 `git push`，把报错信息复制下来；
如果是 `Failed to connect to github.com port 443` / `Connection was reset` 这类，就是代理没配，
按 [坑 1](#坑-1git-连不上网-clone-卡住不动代理问题) 处理。

### Q: `git commit` 报错 `Please tell me who you are`？

A: git 身份没配置。执行这两条（把内容换成你自己的），只需配一次：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的GitHub注册邮箱"
```

### Q: 提交时提示 `nothing to commit, working tree clean`？

A: 说明 Git 没检测到任何改动，常见原因：

1. **文件根本没保存**（VS Code 标签页上有个小圆点 = 未保存，按 `Ctrl+S` 保存）；
2. 改的文件在被 `.gitignore` 忽略的目录里（如 `dist/`、`node_modules/`）；
3. 你以为改了但改在了别的地方——用 `git status` 核对。

### Q: 提交时提示 `failed to push some refs` / `Updates were rejected`？

A: 说明**你 Fork 的远端 `main` 上有你本地没有的提交**（最常见原因：你在 GitHub 网页上直接改过东西，
或者点了 `Sync fork` 同步了上游）。按顺序试：

```bash
# 1. 先把远端的改动拉下来合并
git pull origin main

# 2. 如果弹出编辑器让你写合并说明，直接保存退出即可（vim 按 Esc 然后输入 :wq 回车）
# 3. 再推一次
git push origin main
```

> ⛔ **千万不要**用 `git push -f` 去「解决」这个报错——那会直接覆盖掉远端的提交。
> 如果 `git pull` 后出现冲突（提示 `CONFLICT`）自己处理不了，**停下来找管理员**，把终端输出截图发过去。

### Q: 不小心把不该提交的文件提交了（比如一堆图片、`node_modules`）？

A: **先别慌，也别自己删库**。分两种情况：

- **还没推送**：在 VS Code 源代码管理面板顶部 **···** → **提交 → 撤销上次提交 (Undo Last Commit)**，
  文件会退回「更改」区；然后鼠标移到多余文件上点 **↩ 撤销更改** 把它去掉，再重新提交一次。
  （用命令的话是 `git restore --staged 文件路径` + `git commit --amend`。）
- **已经推送了**：**不要自己折腾历史**（`reset --hard`、`filter-branch` 这些很容易把仓库搞坏）。
  在 PR 里说明情况，或者直接找管理员处理。

### Q: 我把某张图片/某个页面改坏了，怎么恢复到原来的样子？

A: **最简单的方法：用鼠标点**。在 VS Code 源代码管理面板里，鼠标移到那个文件上，点右边的
**↩ 撤销更改 (Discard Changes)**，这个文件就回到上次提交的状态了。

> ⚠️ 这会**永久丢掉你在这个文件里没提交的修改**，点之前想清楚。
> 只想撤销**某一个文件**就精确点那一个，**不要**用「全部撤销」。

如果你的改动**已经提交了**，才需要用到命令（这种属于进阶操作，拿不准就问管理员）：

```bash
# 查看某个文件历史上都改过什么（找到你想回去的那个版本）
git log --oneline doc/xxx.md

# 先看一眼那个版本长什么样，确认是你要的
git show <提交号>:doc/xxx.md

# 确认后再恢复它
git checkout <提交号> -- doc/xxx.md
```

> ⛔ **不要用 `git reset --hard`、不要用 `git checkout .`**：它们会把你**所有**没提交的改动一次性清空，
> 而且**无法恢复**。想撤销就精确到「某一个文件」。

### Q: `pnpm dev` 打不开 / 页面白屏 / 报一堆错？

A: 按顺序排查：

1. **依赖没装**：第一次要用 `pnpm install`（见 [方式二第 5 步 · 本地预览](#5-本地预览想看到和网站一样的效果)）。
2. **`corepack`/`pnpm` 没启用**：`node -v` 有没有输出？`pnpm -v` 有没有输出？
3. **刚改了 `sidebar.mjs` / `nav.mjs`**：这两个是 JavaScript 文件，**少一个逗号就会整个站白屏**。
   终端里通常会指明是哪个文件第几行——把你刚才加的那一行删掉，看能不能恢复，再重新加。
4. **端口被占用**：终端会提示 `Port 5173 is in use`，它会自动换一个端口（如 5174），**以终端实际打印的地址为准**。
5. 都不行：把终端里的报错**完整截图**发给管理员，别只截最后一行。

### Q: 我可以直接在 GitHub 网页上改文件吗？

A: 可以，**但必须提交到你自己的 `main` 分支**，操作见 [方式一](#方式一在线编辑最简单推荐新手)。
不要用网页的 `Create a new branch for this commit` 选项。

---

## 📞 需要帮助？

**求助顺序建议**（从快到慢）：

1. 🤖 **先问 AI Agent**：把报错原文和你的操作贴给它，绝大多数问题当场解决，见 [配合 AI Agent 一起食用](#-小白强烈建议配合-ai-agent-一起食用)。
2. 💬 群里问队友：适合"这个页面该放哪个目录""这张图能不能用"这类需要人来判断的问题。
3. 🐛 提 Issue：https://github.com/SYLU-Ambition/SLDX_Ambition_Website/issues
4. 👤 联系管理员：**@Zhan_Kong**（QQ：2276539496）

**找人问的时候，请一次性提供这三样**（缺了只能来回问，很浪费时间）：

- 你**想做什么**（如：给 24 对抗赛加 5 张照片）
- 你**点了/敲了什么**（如：在 VS Code 里点了提交，然后点推送）
- **报错原文**（截图或复制文字，别只截最后一行；终端报错要包含前面几行）

- 查看现有 PR：https://github.com/SYLU-Ambition/SLDX_Ambition_Website/pulls

---

## 🧾 速查卡（贴在显示器边上那种）

### 只用鼠标的日常流程（90% 的改动都走这条）

```
1. 文件管理器里，把图片拖进 doc/public/Photo/photo_XXXX/，改成下一个编号
2. VS Code 里打开对应的 .md，加一行  ![说明](/Photo/photo_XXXX/50.jpg)
3. VS Code → 源代码管理（Ctrl+Shift+G）
4. 鼠标移到要提交的文件上 → 点 +   （图片和 .md 两个都要点！）
5. 输入框写说明：docs: 更新 XX 内容
6. 点 ✓ 提交 → 点 ··· → 推送
7. 浏览器打开 github.com/你的用户名/SLDX_Ambition_Website
   → Compare & pull request → 核对 base/compare → 填标题说明 → Create pull request
8. 等管理员审核；合并后到你自己仓库点 Sync fork → Update branch
```

### PR 三个「选什么」的对照表（最容易选错的地方）

| 在哪里 | 字段 | 选什么 |
| --- | --- | --- |
| 线上编辑提交时 | 单选按钮 | **`Commit directly to the main branch`**（不要选 Create a new branch） |
| 发起 PR 时 | `base repository` / `base` | `SYLU-Ambition/SLDX_Ambition_Website` / `main` |
| 发起 PR 时 | `head repository` / `compare` | `你的用户名/SLDX_Ambition_Website` / `main` |

完整点法见 [方式二第 8 步 · 在 GitHub 网站上创建 Pull Request](#8-在-github-网站上创建-pull-request逐步图解)。

### 三条铁律

| # | 规则 |
| --- | --- |
| 1 | **只在 `main` 分支上改**，不新建分支（左下角状态栏能看到当前分支） |
| 2 | **只提交自己改过的文件**，别碰 `node_modules/`、`dist/`、`doc/public原图备份/` |
| 3 | **图片必须压到 1MB 以内**再上传，且**不能和别人重名**（会覆盖旧图） |

### 卡住时的第一反应

> 🤖 **别自己硬扛，把报错原文丢给 AI Agent**：`「我想做 XX，点了 XX，报错是 XXX，帮我看看」`
>
> 详见 [小白强烈建议：配合 AI Agent 一起食用](#-小白强烈建议配合-ai-agent-一起食用)。
> 但**提交前自己核对文件清单、不允许它执行删除类操作**。

### 什么时候才需要开终端

- **git 连不上网**（clone/push 卡住报超时）
- **想本地预览**（`pnpm dev` 看效果）
- **图片特别多**，想用脚本批量压缩

```bash
# —— 网络：系统代理模式下必做（端口以你代理软件显示为准）——
git config --global http.https://github.com.proxy http://127.0.0.1:7897
git config --global http.https://github.com/.proxy http://127.0.0.1:7897
git config --global --get-regexp proxy        # 检查是否写进去了

# —— 分支：只用 main，不建新分支 ——
git branch                                     # 确认 * 在 main 上
git checkout main                              # 不在 main 上就切回来

# —— 本地预览 ——
pnpm install                                   # 第一次才需要
pnpm dev                                       # 看完按 Ctrl+C 停掉

# —— 日常：改完 → 提交 → 推送（想看清每步时用）——
git status                                     # 看看改了啥
git add doc/xxx.md                             # 只加自己改过的文件
git commit -m "docs: 更新 XX 内容"
git push origin main                           # 推到你自己 Fork 的 main

# —— 出事时先问管理员，别自己来 ——
# git push -f / git reset --hard / git checkout -b / git checkout --orphan
# 这四条命令新手禁用 ⛔
```

**提交 PR 时：base = `SYLU-Ambition/SLDX_Ambition_Website:main`，compare = `你的用户名/SLDX_Ambition_Website:main`。**

---

**感谢你为战队网站做出的贡献！** 🎉
